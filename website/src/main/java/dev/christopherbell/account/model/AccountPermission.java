package dev.christopherbell.account.model;

/**
 * Stored account capabilities that supplement, but do not replace, role-based authorization.
 */
public enum AccountPermission {
  SHARED_FOLDER_READ,
  SHARED_FOLDER_WRITE
}
